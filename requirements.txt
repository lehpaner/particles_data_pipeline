fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pyyaml>=6.0

# Uncomment if config.yaml sets database.driver: postgresql (not needed for
# the default sqlite driver):
# psycopg2-binary>=2.9
